packages:
  "framer-motion": "For fluid UI animations in chat and dashboard"
  "lucide-react": "For beautiful icons"
  "clsx": "For utility class merging"
  "tailwind-merge": "For merging tailwind classes"
  "date-fns": "For formatting property creation dates"
  "react-hook-form": "For robust form handling in CEO panel"
  "@hookform/resolvers": "For Zod validation integration"
  "zod": "For schema validation"

images:
  - path: "artifacts/rome-guest/public/images/mediterranean-bg.png"
    description: "Soft, elegant, blurred gradient background in pale blue and cream tones, suggesting a luxury hospitality suite"
    aspect_ratio: "16:9"

notes:
  - Using generated hooks from @workspace/api-client-react
  - Routing via Wouter with dynamic /guest/:slug paths
